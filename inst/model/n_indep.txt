model{
  
  for (i in 1:n) {
    y[i] ~ dbinom(theta[i], num[i])
    theta[i] <- (1/(sqrt(2*3.14)*sigma[group[i]]))*exp(-(x[i] - mu[group[i]])^2 / (2*sigma[group[i]]^2))
  }
  
  for (g in 1:nG) {
    mu[g] ~ dnorm(mx, 1/vm)
    sig[g] ~ dgamma(va, vb)
    sigma[g] <- 1/sig[g]
  }
}