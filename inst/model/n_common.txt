model{
  
  for (i in 1:n) {
    y[i] ~ dbinom(theta[i], num[i])
    logit(theta[i]) <- ltheta[i]
    ltheta[i] ~ dnorm(mu, sigma)
  }
  
  mu    ~ dnorm(mx, 1/vm)
  1/sqrt(sigma) <- sig
  sig ~ dt(0, 1/vs, 1) T(0,)
  
  }